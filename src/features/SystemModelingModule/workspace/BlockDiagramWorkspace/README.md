# Block Diagram Workspace

The workspace is the drop target for model components selected from the library.

The first implementation supports placing components on a grid. Later iterations can add ports, wiring, selection state, component configuration, and serialization.
