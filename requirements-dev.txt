# Development dependencies for Engineering System Designer
#
# These are needed for local development, testing, and CI but not at
# runtime. See `09_coding_standards.md` §13 for tool configuration and
# `12_ci_cd_pipeline.md` for CI usage.

# --- Linting and formatting ---------------------------------------------------
ruff==0.7.0

# --- Type checking ------------------------------------------------------------
mypy==1.13.0

# --- Testing ------------------------------------------------------------------
pytest==8.3.3
pytest-cov==5.0.0
pytest-qt==4.4.0
pytest-xdist==3.6.1
pytest-timeout==2.3.1

# --- Pre-commit hooks ---------------------------------------------------------
pre-commit==4.0.1

# --- Security scanning --------------------------------------------------------
safety==3.2.9
bandit==1.7.10

# --- Build tooling ------------------------------------------------------------
build==1.2.2
