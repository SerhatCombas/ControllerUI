# System Modeling Module

Owns the model library and block diagram workspace used to build engineering systems from reusable components.

## Current UI Responsibilities

- `ModelLibraryPanel` lists reusable SVG-based engineering components.
- `BlockDiagramWorkspace` accepts library components through drag and drop.
- Components are grouped by domain, family, and category.

